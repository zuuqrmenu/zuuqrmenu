# Git Workflow Rules

1. **Commit Messages**: All git commit messages MUST strictly be written in English using Conventional Commits format (e.g. `feat: ...`, `fix: ...`, `style: ...`, `refactor: ...`, `chore: ...`).
2. **Push Restrictions**: NEVER run `git push` unless the user explicitly instructs to push to remote/GitHub.
