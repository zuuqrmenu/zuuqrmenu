const SearchSheet = ({ value, onChange, results, onSelect, onClose }) => <div className="public-search-panel"><div className="search-row"><input autoFocus value={value} onChange={(event) => onChange(event.target.value)} placeholder="Ürün ara..." aria-label="Ürün ara" /><button type="button" onClick={() => onChange('')} aria-label="Aramayı temizle">×</button><button type="button" onClick={onClose} aria-label="Aramayı kapat">Kapat</button></div>{value && <div className="search-results">{results.length ? results.map((product) => <button type="button" key={product.id} onClick={() => onSelect(product)}><span>{product.name}</span><strong>₺{Number(product.price).toFixed(2)}</strong></button>) : <p>Aramanızla eşleşen ürün bulunamadı.</p>}</div>}</div>;

export default SearchSheet;